<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Artistrie</title>
</head>
<body>
  <nav class="navbar navbar-inverse navbar-static-top" role="navigation"> 
      <div class="container">
          <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
          </button>
              <a id="homeLink" class="navbar-brand active" href="/">Artistrie</a>
          </div>

          <div class="collapse navbar-collapse navbar-ex1-collapse">
              <ul class="nav navbar-nav">
                  <li>
                      <a href="/contact">Contact</a>
                  </li>
                  <li>
                      <a href="/competitions">Artist Competitions</a>
                  </li>
              </ul>
              <ul class="nav navbar-nav navbar-right">
                  <% if @user = current_user %>
                      <li><a href="/users/<%= @user.id %>"><%= @user.username %>'s Profile</a></li>
                      <li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete %></li>
    <!--        if no logged in user, show these menu items
     -->          <% else %>
                      <li><a href="/users/sign_in">Login</a></li>
                      <li><a href="/users/sign_up">Sign Up</a></li>
                  <% end %>
              </ul>
          </div>
      </div>
  </nav>
	<div class="jumbotron">
		<div class="row">
			<div class="col-xs-10 col-xs-offset-1">
				<h1>Welcome <%=@user.username%> </h1>
			</div>
		</div>
	</div>
	<div ng-app='ArtistrieApp'>
		<div ng-controller='User'>
    <div ng-repeat="post in posts">
      <h4>{{post.title}}</h4> 
		</div>
    </div>
    <div ng-controller="ExampleCtrl">
    <div ng-repeat="piece in pieces"> 
    <p>{{piece.title}}</p>
    <p>{{piece.artist}}</p>
    </div>
    </div>
	</div>	
</body>
</html>